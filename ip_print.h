#ifndef IP_PRINT_H
#define IP_PRINT_H

#include <vector>

using namespace std;

/**
	Print IP address
*/
void PrintIp(const vector<int>& ipAddress);

/**
	Print IP Pool
*/
void IpPoolPrint(const vector<vector<int>>& ipPool);

#endif